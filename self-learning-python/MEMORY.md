# Project memory

## Project context
- Python learning project for self-study
- Started with basic Python grammar on day-001
- Working directory: `/Users/ynwa/Desktop/learning-notes/self-learning-python`

## Discovered durable knowledge

- **Python basics covered in day-001** [ses_1432fdd87ffeD8K4uDYgnZge87]:
  - Variable types: str, int, bool, complex, float, NoneType, list, tuple, dict, set
  - Type detection: `type()` and `isinstance()`; None check uses `x is None`
  - Python has NO block-level scope (unlike JS); variables in `if True:` blocks leak outside
  - `global` keyword modifies module-level variables; `nonlocal` modifies enclosing function scope
  - Closures with `nonlocal` demonstrated via `create_counter()` pattern

- **Learning context** [ses_1432fdd87ffeD8K4uDYgnZge87]:
  - User appears to be learning Python from JavaScript background (my-first-js-project directory exists)
  - Comments and notes are in Chinese
